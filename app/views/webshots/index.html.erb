<!-- <h1>Listing webshots</h1>

<table>
  <thead>
    <tr>
      <th>User</th>
      <th>Title</th>
      <th>Url</th>
      <th>Desc</th>
      <th>Photo</th>
      <th></th>
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @webshots.each do |webshot| %>
      <tr>
        <td><%= webshot.user %></td>
        <td><%= webshot.title %></td>
        <td><%= webshot.url %></td>
        <td><%= webshot.desc %></td>
        <td><%= webshot.photo %></td>
        <td><%= link_to 'Show', webshot %></td>
        <td><%= link_to 'Edit', edit_webshot_path(webshot) %></td>
        <td><%= link_to 'Destroy', webshot, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Webshot', new_webshot_path %>
 -->
<!-- <div class="jumbotron text-center">
  <% if user_signed_in? %>
  <h1>Welcome back, trusted user!</h1>
  <p class="lead">
    You have signed in!  If this were a real app some sweet conent
    would go here.  But it's just a test.  You might want to sign out now.
  </p>
  <%= link_to "Sign Out", destroy_user_session_path, :method => :delete, :class => 'btn btn-large btn-danger' %>
  <% else %>
  <h1>Welcome, new user!</h1>
  <p class="lead">
    You must sign in or sign up before you can access our super sweet,
    top secret content.
  </p>
  <div class="row centred">
    <div class="col-md-6">
      <%= link_to "Sign In", new_user_session_path, :class => "btn btn-lg btn-success" %>
    </div>
    <div class="col-md-6">
      <%= link_to "Sign Up", new_user_registration_path, :class => "btn btn-lg btn-success" %>
    </div>
  </div>
  <% end %>
</div> -->

<%= render 'partial/webshots' %>